# Task S004_10：停止决策

## 任务目标

阅读 `inputs/final_state.json`。在满足全部约束时决定是否继续添加 buffer、upsize driver 或放宽约束。

提交 `answer.json`：

```json
{
  "stop": true,
  "reason": "...",
  "additional_eco": "..."
}
```

