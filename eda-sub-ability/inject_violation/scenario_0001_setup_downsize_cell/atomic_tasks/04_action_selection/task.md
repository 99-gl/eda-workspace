# Task S001_04：ECO 动作选择

根据已确认的对象和候选试验结果，在 `size_cell`、`swap_vt`、`insert_buffer`、`clone_driver`、`adjust_placement` 中选择最小且已被增量 STA 证明有效的动作。只做选择，不修改设计。提交 `answer.json`。

```json
{
  "eco_action": "",
  "cell_mapping": {}
}
```
