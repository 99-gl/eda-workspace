# Task S001_06：ECO 副作用预测

根据 `inputs/eco_plan.json` 和修改前指标，预测并量化黄金 size-cell ECO 对 setup、hold、DRV、面积、功耗和拥塞的影响。不得执行修改。提交 `answer.json`。

方向字段使用 `improve`、`degrade`、`increase`、`decrease` 或 `unchanged`。
