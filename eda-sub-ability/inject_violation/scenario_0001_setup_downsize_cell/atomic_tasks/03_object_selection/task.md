# Task S001_03：ECO 对象选择

从 `initial_state/design.enc` 查询最差 Setup 路径、实例 cell、输出 net、负载和 DRV。选择能够以最小局部 ECO 同时修复 setup、transition 和 capacitance 的完整 instance 集合；只分析，不修改设计。提交 `analyze.tcl`、`reports/object_analysis.rpt` 和 `answer.json`。

```json
{
  "object_type": "instance",
  "object_names": []
}
```
