# 根因诊断

阅读 `inputs/placement.rpt`、`inputs/overlap_objects.tsv`、`inputs/connectivity.rpt` 和 `inputs/drc.rpt`。判断主违例类型、涉及的实例，并给出两项直接证据。将结果写入 `answer.json`，字段为 `root_cause`、`instances`、`evidence`；实例按字典序排列。不要修改任何输入。

